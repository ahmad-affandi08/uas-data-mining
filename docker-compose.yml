version: '3.8'

services:
  app:
    build: .
    container_name: datamining-app
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - STREAMLIT_SERVER_PORT=8501
    restart: unless-stopped
